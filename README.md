# Blog

Hello!

This site is built with Angular 22.

## Development

Use the pnpm version pinned in `package.json`:

```sh
pnpm install --frozen-lockfile
pnpm start
```

Run the checks with `pnpm lint`, `pnpm test --watch=false --browsers=ChromeHeadlessCI`, and `pnpm build`.
